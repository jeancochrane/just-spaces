# setup:
#   addons:
#   - plan: heroku-postgresql
build:
  docker:
    django: Dockerfile
# release:
#   command:
#     - ./bin/migrate
run:
  docker: /usr/local/bin/gunicorn -b :8000 --reload --access-logfile=- --error-logfile=- --log-level=debug justspaces.wsgi
