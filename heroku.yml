setup:
  addons:
   - plan: heroku-postgresql
     as: DATABASE
build:
  docker:
    django: Dockerfile
run:
  django: "/usr/local/bin/gunicorn -b :8000 --reload --access-logfile=- --error-logfile=- --log-level=debug justspaces.wsgi"
