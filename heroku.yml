setup:
  addons:
   - plan: heroku-postgresql
     as: DATABASE
build:
  docker:
    web: Dockerfile
run:
  web: "/usr/local/bin/gunicorn -b :8000 --reload --access-logfile=- --error-logfile=- --log-level=debug justspaces.wsgi"
